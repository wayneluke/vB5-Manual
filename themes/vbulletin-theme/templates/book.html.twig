{% extends 'docs.html.twig' %}

{% block content %}
	<div id="chapter">
    	<div id="body-inner">
			<div>
				{{ page.content }}
				<hr>
				<h2>Chapters in this book:</h2>
				<ul>
				{% for child in page.collection %}
    				<li><a href="{{ child.url }}">{{ child.title }}</a></li>
				{% endfor %}
				<ul>
			</div>
		</div>
    </div>
{% endblock %}
